# src/commands/secrets.yaml

description: >
  under construction: can pass credentials and a target to retrieve a list of secrets to add to ENV

parameters:

  secrets:
    description: file containing json formatted list of the keys (secrets) to retrieve and set to ENV
    type: string

  store:
    description: identifier for supported secret stores
    type: enum
    enum: ["secrets-manager", "vault"]

steps:
  - run:
      name: pending
      command: |
        echo 'under development'
        echo 'retrive << parameters.secrets >> from << parameters.store >>
